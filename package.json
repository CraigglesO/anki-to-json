{
  "name": "simple-service-discovery-protocol",
  "version": "1.0.5",
  "description": "When creating new NodeJS modules, use this skeletal structure",
  "main": "lib/index.js",
  "scripts": {
    "lint+build+test": "yarn run lint && yarn run build && yarn run test",
    "lint": "echo linting... && eslint ./src/**/*.js && echo 'successfully linted'",
    "build": "echo building... && babel src/ -d lib/ && echo 'successfully built'",
    "prepublish": "yarn run build",
    "test": "echo running server tests... && tape test/**/*.js | tap-summary && echo 'successfully completed tests'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CraigglesO/simple-service-discovery-protocol.git"
  },
  "author": "Craig OConnor",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/CraigglesO/simple-service-discovery-protocol/issues"
  },
  "homepage": "https://github.com/CraigglesO/simple-service-discovery-protocol#readme",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/plugin-syntax-flow": "^7.2.0",
    "@babel/preset-env": "^7.2.3",
    "@babel/preset-flow": "^7.0.0",
    "babel-eslint": "^10.0.1",
    "eslint": "^4.0.0",
    "express": "^4.16.4",
    "flow-bin": "^0.48.0",
    "tape": "^4.6.3"
  },
  "dependencies": {
    "nedb": "^1.8.0",
    "uuid": "^3.3.2"
  }
}
